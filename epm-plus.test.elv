>> 'Installing from a Git repository' {
  >> 'when requesting no specific version' {
    >> 'should perform a basic clone from main' {
      fail-test
    }
  }

  >> 'when requesting a specific version' {
    >> 'should clone to that version' {
      fail-test
    }
  }
}